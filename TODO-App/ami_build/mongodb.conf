 mongod.conf

# for documentation of all options, see:
#   http://docs.mongodb.org/manual/reference/configuration-options/

# where to write logging data.
systemLog:
  destination: file
  logAppend: true
  path: /var/log/mongodb/mongod.log

# Where and how to store data.
storage:
  dbPath: /var/lib/mongo
  journal:
    enabled: true
#  engine:
#  wiredTiger:

# how the process runs
processManagement:
  fork: true  # fork and run in background
  pidFilePath: /var/run/mongodb/mongod.pid  # location of pidfile
  timeZoneInfo: /usr/share/zoneinfo

# network interfaces
# net:
#   port: 27017
#   bindIp: 127.0.0.1  # Enter 0.0.0.0,:: to bind to all IPv4 and IPv6 addresses or, alternatively, use the net.bindIpAll setting.
net:
  port: 27017
  bindIp: 0.0.0.0
  tls:
    mode: requireTLS
    certificateKeyFile: /opt/mongodb/ssl/mongod.pem
    FIPSMode: true
  unixDomainSocket:
    pathPrefix: /opt/mongodb/tmp
    filePermissions: 0766

#security:

#operationProfiling:

#replication:

#sharding:

## Enterprise-Only Options

#auditLog:

# net:
#   port: 27017
#   bindIp: 0.0.0.0
#   tls:
#     mode: requireTLS
#     certificateKeyFile: /opt/mongodb/ssl/mongod.pem
#   unixDomainSocket:
#     pathPrefix: /opt/mongodb/tmp
#     filePermissions: 0766

security:
  authorization: enabled

# processManagement:
#   fork: true

setParameter:
  enableLocalhostAuthBypass: false
---
# mongod.conf

# for documentation of all options, see:
#   http://docs.mongodb.org/manual/reference/configuration-options/

# where to write logging data.
systemLog:
  destination: file
  logAppend: true
  path: /opt/mongodb/log/mongod.log

# Where and how to store data.
storage:
  dbPath: /var/lib/mongo
  journal:
    enabled: true
#  engine:
#  wiredTiger:

# how the process runs
processManagement:
  fork: true  # fork and run in background
  pidFilePath: /var/run/mongodb/mongod.pid  # location of pidfile
  timeZoneInfo: /usr/share/zoneinfo

# network interfaces
#net:
#  port: 27017
#  bindIp: 127.0.0.1  # Enter 0.0.0.0,:: to bind to all IPv4 and IPv6 addresses or, alternatively, use the net.bindIpAll setting.
net:
  port: 27017
  bindIp: 0.0.0.0
  tls:
    mode: requireTLS
    certificateKeyFile: /opt/mongodb/ssl/mongod.pem
    FIPSMode: true
#  unixDomainSocket:
#    pathPrefix: /opt/mongodb/tmp
#    filePermissions: 0766

security:
  authorization: enabled

#operationProfiling:

#replication:

#sharding:

## Enterprise-Only Options

#auditLog:

#snmp:

setParameter:
  enableLocalhostAuthBypass: false